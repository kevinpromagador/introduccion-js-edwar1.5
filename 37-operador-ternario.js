// el operador ternario en JS es una forma corta y elegante de una estrcutura if ..else 
    
    // se llama ternario porque tiene 3 operadores, obervemos la sintaxis 
    // condicion ? valorSiVerdadeero : Valorsifalso

    //ejemplo 1

    let age = 18;
    let message = age >= 18 ? "eres mayor de edad" : " si no erees mayor de edad"

    console.log(message);